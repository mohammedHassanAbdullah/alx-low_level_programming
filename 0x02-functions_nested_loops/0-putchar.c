#include "main.h"

/**
 * main - Entry point
 * 0-putchar.c
 * Return: Always 0 (Success)
 */
int main(void)
{
	char p[] = "_putchar";
	int index;

	for (index = 0; index <= sizeof(p); index++)
	{
		_putchar(p[index]);
	}
	-putchar('\n';)
	return (0);
}
