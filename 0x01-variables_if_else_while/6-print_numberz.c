#include <stdio.h>

/**
 * main - Entry point
 * Alpha bit
 * Return: Always 0 (Success)
 */
int main(void)
{
	int alpha;

	for (alpha = 48; alpha < 58; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
