#include <stdio.h>

/**
 * main - Entry point
 * Alpha bit
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha;

	for (alpha = 0; alpha < 10; alpha++)
	{
		printf("%d", alpha);
	}
	putchar('\n');
	return (0);
}
