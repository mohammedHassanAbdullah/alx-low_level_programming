#include "main.h"

/**
 * mul -> a function that multiplies two integers
 *
 * @a: is integr argument
 * @b: is integr argument
 *
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
